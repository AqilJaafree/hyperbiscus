# ── Claude API ────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── Solana ────────────────────────────────────────────────────────────────────
SOLANA_RPC_URL=https://api.devnet.solana.com

# ── Session key (the "ESP32 key" — signs update_lp_status each tick) ──────────
# Path to a Solana keypair JSON file (array of 64 bytes). REQUIRED.
# In production this lives in ESP32 encrypted NVS storage.
SESSION_KEYPAIR_PATH=~/.config/solana/id.json

# ── WebSocket auth secret ──────────────────────────────────────────────────────
# Optional shared secret for WebSocket auth. If set, mobile clients must send
# { type: "auth", token: "<WS_SECRET>" } as first message or get disconnected.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Leave empty for open access (dev/localhost only).
WS_SECRET=

# ── On-chain accounts (from `anchor test` output or register_lp_monitor TX) ──
# AgentSession PDA address
SESSION_PDA=<session-pda-pubkey>

# Meteora DLMM pool (LbPair) address
LB_PAIR=<lb-pair-pubkey>

# DLMM position account to monitor
POSITION_PUBKEY=<position-pubkey>

# ── Agent behaviour ───────────────────────────────────────────────────────────
# How often to run the monitoring loop (milliseconds). Default: 30s
CHECK_INTERVAL_MS=30000

# Claude model to use. Haiku is fast + cheap for a monitoring loop.
CLAUDE_MODEL=claude-haiku-4-5-20251001
